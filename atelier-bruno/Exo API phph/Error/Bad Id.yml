info:
  name: Bad Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/bookings/:id"
  params:
    - name: id
      value: abc
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
